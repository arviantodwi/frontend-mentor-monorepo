{
  "name": "l1-qrcode",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "live-server --port=5500 --open=./src --watch=./src --wait=500"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
